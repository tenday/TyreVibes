<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="Theme.TyreVibes" parent="android:Theme.Material.Light.NoActionBar">
    </style>

    <!-- Splash Screen Theme. -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/custom_background_color</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/tyrevibes_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.TyreVibes</item>
    </style>
</resources>
